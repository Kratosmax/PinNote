## PinNote 0.6.2

- 修复提醒设置中时、分、秒下拉框与单位文字重叠。
- 修复提醒强度按钮底边被父级布局裁切，统一按钮四边框和间距。
- 普通提醒改为只闪动现有窗口边框，不创建额外提醒窗口。
- 完善待办分组、层级待办、独立窗口、批量提醒、逾期状态和拖动排序。
## PinNote 0.6.1

`0.6.0` 的错误发生在新版更新器启动之前，无法通过自动更新安装本版本。请从本页面手动下载 Setup 覆盖安装，或退出 PinNote 后用 Portable ZIP 替换原程序文件；便签和设置不会被删除。完成这次手动升级后，后续自动更新恢复正常。

- 修复自动更新下载完成后 `package.zip.download` 仍被写入流占用，导致包校验报“文件正由另一进程使用”的问题。
- 更新包现在会在关闭写入句柄后再执行 SHA-256、版本、通道和包结构校验，随后原子改名为 `package.zip`。
- 下载失败会清理临时文件，后续代理线路可立即重试，不会被上一线路遗留的文件句柄阻断。

## PinNote 0.6.0

- 新增文字调色板：永久保留深色、青绿、珊瑚红，并提供 3 个全局常用色槽。
- 自选颜色会自动去重并按最近使用顺序保存，超过 3 个时淘汰最旧颜色；所有便签即时同步。
- 提醒时间改为时、分、秒精确选择，并为时间和提醒强度选择增加轻量反馈动画。
- 四种提醒强度增加悬浮动作说明，直接展示是否弹窗、通知、抢焦点和持续闪动。
- 统一桌面、安装器和托盘图标，补齐 16 到 256 像素的原生尺寸，并让托盘按系统小图标尺寸加载。

## PinNote 0.5.0

- 修复 Windows 11 毛玻璃被高不透明灰层遮挡的问题：完整启用 DWM 全客户区背景，并检查原生返回码，失败时自动回退。
- 移除便签窗口的重复圆角裁剪，改善顶部黑边、圆角锯齿和非整数 DPI 下的边缘表现。
- 新增 GitHub URL 前缀线路和 HTTP 网络代理；支持 0 到 10 优先级、稳定故障转移、禁用直连和使用未保存配置检查更新。
- 所有更新线路继续执行相同的 RSA 清单验签、SHA-256、版本、通道和包结构校验；失败线路的临时下载会被清理。
- 设置页面改为统一的双标签布局，并新增网络线路管理列表。
- 应用 Logo 改为更轻盈的淡蓝与淡绿色方案。

Windows 11 22621 及以上使用系统 DWM 背景材质；Windows 10、较早的 Windows 11 或原生调用失败时使用清晰的不透明回退。GitHub URL 前缀线路会把完整 GitHub 下载 URL 暴露给所配置的第三方服务；HTTP 线路不提供传输保密，建议优先使用 HTTPS 前缀。

当前安装包尚未使用 Windows Authenticode 代码签名，Windows 可能显示未知发布者提示。自动更新包仍使用 RSA 签名清单和 SHA-256 校验。
